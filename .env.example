# SMTP Configuration (Gmail)
# Use your full Gmail address and an App Password (not your login password)
SMTP_EMAIL=your_email@gmail.com
SMTP_PASSWORD="your_app_password_here"

# Firebase Service Account (JSON string)
# Get this from Firebase Console -> Project Settings -> Service Accounts -> Generate Private Key
# Minify the JSON to a single line before pasting
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"your-project-id",...}
